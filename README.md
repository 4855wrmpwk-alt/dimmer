LED Диммер: Детальное описание кода на Python

📁 Общая структура проекта

text
LightDimmerController/
├── light_dimmer.py      # Основной файл с классом контроллера
├── dimmer_settings.json # Файл сохранения настроек (автосоздается)
└── README.md           # Документация проекта
🏗️ Архитектура приложения

Основной класс: LightDimmerController

python
class LightDimmerController:
    def __init__(self, root):
        # Инициализация всех компонентов
        self.root = root
        self.brightness = 50
        self.is_on = False
        self.connection_status = False
        self.simulation_mode = True
Что делает этот блок:

Создает главный класс приложения
Инициализирует начальные параметры:

brightness - текущая яркость (0-100%)
is_on - состояние света (вкл/выкл)
connection_status - статус подключения к устройству
simulation_mode - режим работы (симуляция/реальное устройство)
Настраивает цветовую схему для темного оформления
🎨 Блок 1: Настройка графического интерфейса

1.1 Инициализация главного окна

python
def setup_ui(self):
    self.root.title("Контроллер диммера света")
    self.root.geometry("800x600")
    self.root.configure(bg='#1a1a1a')
Пояснение:

title() - устанавливает заголовок окна
geometry() - задает размеры окна (800x600 пикселей)
configure(bg=...) - устанавливает темный фон окна
Используется шестнадцатеричный код цвета #1a1a1a (темно-серый)
1.2 Создание заголовка

python
title_label = tk.Label(
    header_frame,
    text="💡 Управление диммером света",
    font=('Arial', 24, 'bold'),
    fg='#00a8ff',  # Цвет текста (голубой)
    bg='#1a1a1a'   # Цвет фона
)
Ключевые параметры:

font=('Arial', 24, 'bold') - шрифт, размер, жирность
fg='#00a8ff' - цвет текста (foreground)
bg='#1a1a1a' - цвет фона (background)
pack() - метод размещения виджета в окне
🔌 Блок 2: Панель подключения

2.1 Выбор режима работы

python
self.mode_var = tk.StringVar(value="simulation")

tk.Radiobutton(
    mode_frame,
    text="Симуляция",
    variable=self.mode_var,
    value="simulation",
    command=self.toggle_mode
)
Как это работает:

StringVar() - специальная переменная Tkinter для хранения значений
Radiobutton - переключатель выбора
variable=self.mode_var - связь с общей переменной
value="simulation" - значение, которое будет установлено при выборе
command=self.toggle_mode - функция, вызываемая при изменении
2.2 Подключение к COM-порту

python
self.port_combo = ttk.Combobox(
    self.device_frame,
    textvariable=self.port_var,
    width=15,
    state='readonly'
)
self.port_combo['values'] = ['COM1', 'COM2', 'COM3', 'COM4', 'COM5']
Компоненты:

Combobox - выпадающий список
state='readonly' - запрет ручного ввода
values - доступные варианты выбора
ttk - модуль для стилизованных виджетов (более современный вид)
🎛️ Блок 3: Управление светом

3.1 Кнопка включения/выключения

python
self.power_btn = tk.Button(
    left_panel,
    text="ВКЛЮЧИТЬ",
    command=self.toggle_power,
    font=('Arial', 16, 'bold'),
    bg='#e84118',  # Красный цвет для выключенного состояния
    fg='white',
    height=2,
    width=15
)
Логика работы:

python
def toggle_power(self):
    self.is_on = not self.is_on  # Меняем состояние на противоположное
    
    if self.is_on:
        self.power_btn.config(text="ВЫКЛЮЧИТЬ", bg='#4cd137')  # Зеленый
        self.log_event("Свет включен")
    else:
        self.power_btn.config(text="ВКЛЮЧИТЬ", bg='#e84118')  # Красный
        self.log_event("Свет выключен")
3.2 Слайдер яркости

python
self.brightness_slider = tk.Scale(
    brightness_frame,
    from_=0,
    to=100,
    orient=tk.HORIZONTAL,
    length=300,
    command=self.update_brightness
)
Параметры слайдера:

from_=0, to=100 - диапазон значений
orient=tk.HORIZONTAL - горизонтальная ориентация
length=300 - длина в пикселях
command=self.update_brightness - функция, вызываемая при движении слайдера
Функция обновления яркости:

python
def update_brightness(self, value):
    self.brightness = int(value)  # Преобразуем строку в число
    self.brightness_label.config(text=f"{self.brightness}%")
    
    if self.is_on:
        self.send_command()  # Отправляем команду устройству
        self.update_light_visualization()  # Обновляем отображение
🖼️ Блок 4: Визуализация света

4.1 Создание графического элемента

python
self.light_canvas = tk.Canvas(
    right_panel,
    bg='black',
    width=300,
    height=300
)

self.light_circle = self.light_canvas.create_oval(
    50, 50, 250, 250,  # x1, y1, x2, y2 (координаты)
    fill='#333333',     # Начальный цвет (серый)
    outline=''          # Без контура
)
Координаты овала:

(50, 50) - верхний левый угол
(250, 250) - нижний правый угол
Создает круг диаметром 200 пикселей
4.2 Динамическое изменение цвета

python
def update_light_visualization(self):
    if not self.is_on:
        self.light_canvas.itemconfig(self.light_circle, fill='#333333')
    else:
        # Вычисляем цвет в зависимости от яркости
        intensity = self.brightness / 100.0
        r = int(255 * intensity)          # Красный компонент
        g = int(255 * intensity * 0.8)    # Зеленый (немного меньше)
        b = int(100 * intensity)          # Синий (минимально)
        
        color = f'#{r:02x}{g:02x}{b:02x}'  # Формируем HEX-цвет
        self.light_canvas.itemconfig(self.light_circle, fill=color)
Формула расчета цвета:

intensity = brightness / 100 - нормализуем яркость от 0 до 1
Каждый цветовой канал умножается на intensity
:02x форматирует число в шестнадцатеричный вид с двумя символами
🔧 Блок 5: Работа с устройствами

5.1 Режим симуляции

python
def send_command(self):
    """Отправка команды на устройство"""
    if self.simulation_mode:
        state = "вкл" if self.is_on else "выкл"
        self.log_event(f"Команда: {state}, яркость: {self.brightness}%")
Что происходит в симуляции:

Команды не отправляются на реальное устройство
Все действия логируются для отладки
Пользователь видит результат в интерфейсе
5.2 Режим реального устройства

python
def connect_device(self):
    if self.simulation_mode:
        messagebox.showinfo("Информация", "В режиме симуляции подключение не требуется")
        return
        
    port = self.port_var.get()
    
    # Симуляция подключения (в реальности здесь будет pyserial)
    self.connection_status = True
    self.status_label.config(text=f"🟢 Подключено к {port}")
Протокол обмена с устройством:

python
# Формат команды: P{состояние}B{яркость}
# Пример: P1B075 - включить с яркостью 75%
command = f"P{power}B{self.brightness:03d}"
# :03d - форматирует число как трехзначное (075 вместо 75)
💾 Блок 6: Сохранение и загрузка настроек

6.1 Структура файла настроек

json
{
  "brightness": 75,
  "is_on": true,
  "simulation_mode": false,
  "port": "COM3"
}
6.2 Сохранение настроек

python
def save_settings(self):
    settings = {
        'brightness': self.brightness,
        'is_on': self.is_on,
        'simulation_mode': self.simulation_mode,
        'port': self.port_var.get()
    }
    
    with open('dimmer_settings.json', 'w') as f:
        json.dump(settings, f, indent=4)  # indent=4 для красивого форматирования
Ключевые моменты:

json.dump() - записывает Python-словарь в JSON-файл
indent=4 - добавляет отступы для читаемости
with open() as f: - гарантирует закрытие файла после записи
6.3 Загрузка настроек

python
def load_settings(self):
    try:
        if os.path.exists('dimmer_settings.json'):
            with open('dimmer_settings.json', 'r') as f:
                settings = json.load(f)  # Чтение JSON в словарь
            
            # Восстанавливаем параметры
            self.brightness = settings.get('brightness', 50)  # 50 - значение по умолчанию
            self.is_on = settings.get('is_on', False)
            
            # Обновляем интерфейс
            self.brightness_slider.set(self.brightness)
    except Exception as e:
        self.log_event(f"Ошибка загрузки настроек: {str(e)}")
📝 Блок 7: Система логирования

7.1 Формат записи логов

python
def log_event(self, message):
    timestamp = datetime.now().strftime("%H:%M:%S")
    log_message = f"[{timestamp}] {message}\n"
    
    self.log_text.insert(tk.END, log_message)  # Добавляем в текстовое поле
    self.log_text.see(tk.END)  # Прокручиваем к последней записи
Компоненты:

datetime.now() - текущее время
strftime("%H:%M:%S") - формат "часы:минуты:секунды"
tk.END - конец текстового поля
see(tk.END) - автоматическая прокрутка
🚀 Блок 8: Дополнительные функции

8.1 Авторегулировка яркости

python
def auto_adjust(self):
    def adjust():
        if not self.is_on:
            self.is_on = True
            self.power_btn.config(text="ВЫКЛЮЧИТЬ", bg='#4cd137')
            
        for i in range(0, 101, 10):  # От 0 до 100 с шагом 10
            self.brightness = i
            self.brightness_slider.set(i)
            self.update_light_visualization()
            time.sleep(0.2)  # Пауза 0.2 секунды
Важные аспекты:

Используется threading.Thread для работы в фоне
thread.daemon = True - поток завершится с основным приложением
time.sleep(0.2) - создает плавную анимацию
8.2 Функция расписания

python
def show_schedule(self):
    schedule_window = tk.Toplevel(self.root)  # Создаем новое окно
    schedule_window.title("Расписание")
    schedule_window.geometry("400x300")
Отличие Toplevel от Tk:

Tk() - главное окно приложения
Toplevel() - дочернее окно
Можно создавать несколько Toplevel окон
🔄 Блок 9: Организация кода

9.1 Основной цикл программы

python
def main():
    root = tk.Tk()  # Создаем главное окно
    app = LightDimmerController(root)  # Создаем экземпляр контроллера
    
    # Центрирование окна на экране
    root.update_idletasks()
    width = root.winfo_width()
    height = root.winfo_height()
    x = (root.winfo_screenwidth() // 2) - (width // 2)
    y = (root.winfo_screenheight() // 2) - (height // 2)
    root.geometry(f'{width}x{height}+{x}+{y}')
    
    root.mainloop()  # Запускаем основной цикл обработки событий
Центрирование окна:

update_idletasks() - обновляет информацию о размерах окна
winfo_width()/winfo_height() - получаем размеры окна
winfo_screenwidth()/winfo_screenheight() - получаем размеры экрана
Вычисляем координаты для центрирования
9.2 Точка входа в программу

python
if __name__ == "__main__":
    main()
Зачем это нужно:

Код выполняется только при прямом запуске файла
Позволяет импортировать класс LightDimmerController в других модулях
Стандартная практика в Python
🛠️ Практические советы по расширению

Добавление нового пресета:

python
# В методе setup_ui, в блок presets добавить:
presets = [
    ("Ночник", 10, "#3498db"),
    ("Чтение", 60, "#2ecc71"),
    ("Работа", 80, "#f1c40f"),
    ("Максимум", 100, "#e74c3c"),
    ("Кино", 20, "#9b59b6")  # Новый пресет
]
Изменение цветовой схемы:

python
self.colors = {
    'bg': '#0a0a0a',           # Более темный фон
    'fg': '#f0f0f0',           # Более светлый текст
    'accent': '#1e90ff',       # Другой акцентный цвет
    'on': '#32cd32',           # Цвет включенного состояния
    'off': '#dc143c',          # Цвет выключенного состояния
    'panel': '#2a2a2a'         # Цвет панелей
}
Добавление поддержки горячих клавиш:

python
def setup_shortcuts(self):
    self.root.bind('<Control-s>', lambda e: self.save_settings())
    self.root.bind('<Control-l>', lambda e: self.load_settings())
    self.root.bind('<space>', lambda e: self.toggle_power())
    self.root.bind('<Escape>', lambda e: self.root.quit())
📊 Таблица основных методов класса

Метод	Назначение	Возвращаемое значение
toggle_power()	Включение/выключение света	Нет
update_brightness()	Обновление яркости	Нет
send_command()	Отправка команды устройству	Нет
log_event()	Запись в журнал	Нет
save_settings()	Сохранение настроек	Нет
load_settings()	Загрузка настроек	Нет
update_light_visualization()	Обновление отображения	Нет
